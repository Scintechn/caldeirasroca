# Server-only secrets. NEVER prefix with NEXT_PUBLIC_.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Optional — only used if the Google Reviews block is enabled (lib/reviews.ts).
GOOGLE_PLACES_API_KEY=
GOOGLE_PLACE_ID=

# Public — must be safe to ship to the browser.
NEXT_PUBLIC_GOOGLE_MAPS_EMBED_KEY=
NEXT_PUBLIC_SITE_URL=https://assistencia-caldeira-roca.pt
